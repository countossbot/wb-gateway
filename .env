# 数据库位置（Prisma SQLite）：本机/容器首启由 prisma/init.sql 自动建表
# 注意：Prisma 以 prisma/schema.prisma 所在目录为基准解析相对路径，
# 故此处使用绝对路径，避免解析到 prisma/db/custom.db（不存在）。
DATABASE_URL=file:/Volumes/stable/Github/universal-ai-gateway/db/custom.db
