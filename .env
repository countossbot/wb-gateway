# 数据库位置（Prisma SQLite）：首启由 prisma/init.sql 自动建表
# 注意：Prisma 以 prisma/schema.prisma 所在目录为基准解析相对路径，
# 故相对路径需写为 file:../db/custom.db（等价于项目根的 db/custom.db）。
# 使用相对路径可保证在不同机器/容器上均可直接运行。
DATABASE_URL=file:../db/custom.db
