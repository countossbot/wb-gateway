# 数据库位置（Prisma SQLite）：容器/本机首启由 prisma/init.sql 自动建表
# 注意：Prisma 以 prisma/schema.prisma 所在目录为基准解析相对路径，
# 因此本地部署的相对路径应写成 file:../db/custom.db（等价于项目根的 db/custom.db）；
# 容器/生产环境可用绝对路径，例如 file:/app/db/custom.db。
DATABASE_URL=file:../db/custom.db
# 可选：上游重试基准延迟毫秒数（默认 600）
# RETRY_BASE_MS=600
